# Para evitar SMTP en Render:
USE_RESEND=true
RESEND_API_KEY=re_XXXXXXXXXXXXXXXXXXXXXXXX

# Direcciones
MAIL_FROM="Radicación Preventiva IPS" <no-reply@tudominio.com>   # dominio verificado en Resend
MAIL_TO=contabilidad@tu-dominio.com

# Si algún día quieres volver a SMTP (fallback):
SMTP_HOST=smtp.gmail.com      # o smtp.gmail.com
SMTP_PORT=587                    # 587 (STARTTLS) / 465 (TLS)
SMTP_SECURE=false                   # true solo si usas 465
SMTP_USER=facturapreventiva8@gmail.com
SMTP_PASS=pcansenydlkqxpko

# CORS (si el front vive fuera del mismo host)
CORS_ORIGIN=https://preventiva-radicacion.onrender.com

# Límite total
MAX_TOTAL_MB=40
